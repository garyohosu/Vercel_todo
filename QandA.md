# QandA（未確定事項・設計判断ログ）

## 解決済み

| # | 問い | 採用方針 | 根拠 |
|---|------|----------|------|
| Q-01 | v1 の絞り込み・検索はフロント側か API 側か？ | **フロントエンド側** | 「小さく作れる」「数百件程度」の方針に合わせ GET /api/todos は全件取得のみ。v2 でクエリパラメーター追加可能な構造を維持する |
| Q-02 | ワンクリック完了切替で `doing` タスクはどうなるか？ | **doing → done に変更する** | SPEC「一覧からワンクリックで完了」を優先。`done → todo` に戻す操作も一覧から可能。`doing` への変更は編集画面のみ |
| Q-03 | 「今日まで」フィルターの条件は？ | **due_date <= 今日** | 「今日まで」なら当日を含む締切未消化タスクを全て表示するのが自然 |
| Q-04 | due_date = null タスクの並び順は？ | **未完了グループの末尾（その後 created_at 降順）** | 締切なしタスクは急ぎではないとみなして末尾に置く |
| Q-08 | PUT /api/todos/:id の status 遷移制約はどこに適用するか？ | **遷移制約は UI 側のみ（トグルボタン）。PUT API は有効な status enum 値であれば任意を受け入れる** | SEQUENCE.md SEQ-04 の NOTE を修正済み（クライアント側管理に統一） |

## 未解決（確認が必要）

| # | 問い | 暫定方針 | 確認要否 |
|---|------|----------|----------|
| Q-05 | UI スタイリングライブラリは何を使う？ | **Tailwind CSS（暫定）** | 要確認 |
| Q-06 | Neon の接続情報の環境変数名は？ | **`DATABASE_URL`（暫定）** | 要確認 |
| Q-07 | 入力文字数制限（タスク名 200 文字・説明 1000 文字）は SPEC に追記してよいか？ | **USECASE.md に記載済み。SPEC.md にも追記してよければ追記する** | 要確認 |
| Q-09 | 検索/絞り込みで結果 0 件になった場合、「タスクがありません」メッセージを同様に表示するか？ | **暫定: はい。元データ 0 件・フィルタ後 0 件どちらも同じメッセージを表示する** | 要確認 |

## Codex レビュー指摘ログ

### TESTCASE.md レビュー（2026-04-13）

| 優先度 | 指摘 | 対応 |
|--------|------|------|
| High | TC-03 状態遷移が一覧トグルと編集画面で混在 | TC-03 を「一覧トグル」「編集画面から」に分離。遷移制約はトグルにのみ適用と明記。Q-08 に積み残し |
| High | API テストの `due_date` が JSON 契約の `dueDate` と矛盾 | TC-02 と TC-04 の API ケースを `dueDate` に修正 |
| High | PUT バリデーション（400系）が未テスト | TC-04 に API バリデーション節（4-6〜4-8）を追加 |
| Medium | 編集画面の初期ロード失敗・保存失敗が未テスト | TC-04 例外系に 4-9, 4-10 を追加 |
| Medium | 一覧取得失敗・削除失敗の UI 挙動が未テスト | TC-01 に 1-9 追加。TC-05 に 5-4 追加 |
| Medium | ソート・表示項目の検証が不足 | TC-01 に 1-4〜1-7（created_at 順、─表示、作成日時）を追加 |
| Medium | 絞り込み境界条件が未テスト | TC-06 に 6-9, 6-10 追加。TC-08 に 8-2 追加 |
| Medium | TC-09 期限切れバッジが doing を考慮していない | 9-2 を doing ケースに追加、9-3〜9-5 に番号変更 |

### Phase 6 整合レビュー（2026-04-13）

| 優先度 | 指摘 | 対応 |
|--------|------|------|
| P1 | PUT status 遷移制約が SEQUENCE/CLASS と UI/TESTCASE で衝突 | CLASS.md の `validateStatusTransition` を削除。SEQUENCE.md SEQ-04 NOTE と CLASS.md 責務説明を「UI 側管理」に修正。Q-08 として解決済み |
| P1 | UI.md 画面遷移図: S-03 が 404 エラーにも遷移、保存失敗が S-01 に遷移する記述が詳細仕様と矛盾 | 遷移図を修正: 保存失敗→S-02 留まり、ID不明→S-02 インラインエラー、S-03 はシステムエラーのみ |
| P2 | POST /api/todos の status が USECASE/UI（未着手固定）と矛盾 | SEQUENCE.md POST 説明と SEQ-02 から status を削除、サーバー側で `todo` 固定に修正。CLASS.md `CreateTodoInput` からも削除 |
| P2 | CLASS.md コレクション型: `+todos Todo` / `findAll() Todo` が単数 | `Todo[]` に修正 |
| P2 | 日付判定タイムゾーン基準が SEQUENCE.md にのみ記載 | USECASE.md UC-06 と UI.md 検索ボックス節に「ブラウザのローカルタイムゾーン」を追記 |
| P2 | API エラーレスポンス仕様が未定義 | SEQUENCE.md に API レスポンス仕様テーブル（200/201/400/404/500）を追加 |
| P3 | SEQ-03 の成功分岐が SEQ-02 と二重定義 | `Note` で「成功時は SEQ-02 と同じフロー」と明記 |
| P3 | CLASS.md メンバー記法が非標準寄り | `Todo[]` 修正で一部改善。完全正規化は実装フェーズに委ねる |
| OK | GitHub 上での Mermaid 表示 | 致命的な構文崩れなし。標準機能のみ使用 |

### UI.md レビュー（2026-04-13）

| 優先度 | 指摘 | 対応 |
|--------|------|------|
| P1 | `created_at` がタスク行の表示項目に含まれていない | タスク行テーブルに「作成日時」列を追加 |
| P1 | ソート順が UI.md に記載なし | 「一覧の表示ルール」節を S-01 に追加 |
| P1 | 空状態・エラー状態の UI 定義が不足 | S-01・S-02 に「例外状態」節を追加 |
| P2 | 状態切替ルールが「クリックで状態切替」と曖昧 | タスク行テーブルのチェックボックス行に todo/doing→done、done→todo を明記 |
| P2 | 編集画面の初期ロードフロー（GET/loading/404/error）が未定義 | S-02 に「初期データ読み込み」節を追加 |
| P2 | SPEC.md の GET /api/todos「検索条件付き取得」が USECASE/SEQUENCE と矛盾 | SPEC.md を「v1 は全件取得のみ」に修正 |
| P2 | UC-02 概要「状態を入力して登録」が UI/SPEC の hidden 扱いと矛盾 | USECASE.md の UC-02 概要を「状態は未着手で登録」に修正 |
| P2 | フォーム送信の成否分岐（バリデーション→API→成功/失敗）が図に未定義 | 「フォーム操作の成否フロー」Mermaid flowchart を追加 |
| P3 | アクセシビリティ要件が最小限 | 初版スコープとして据え置き（`aria-label` + ローディング無効化を記載済み） |

### USECASE.md レビュー（2026-04-13）

| 優先度 | 指摘 | 対応 |
|--------|------|------|
| P1 | 検索・絞り込み責務が USECASE と SPEC で衝突 | USECASE.md を修正（フロント側に統一）。Q-01 として解決 |
| P1 | ワンクリック状態変更の遷移規則が未定義 | USECASE.md に明記。Q-02 として解決 |
| P2 | 「今日まで」条件が `= 今日` になっていた | `<= 今日` に修正。Q-03 として解決 |
| P2 | Mermaid の include/extend が誤解を招く | 矢印を削除して単純な flowchart に修正 |
| P2 | due_date = null の並び順が未定義 | UC-01 に暫定方針を明記。Q-04 として解決 |
| P2 | 例外シナリオが不足 | 各 UC 詳細に代替・例外シナリオを追記 |
| P2 | Mermaid 日本語ラベルに quotes が必要 | `user["ユーザー"]` 形式に修正 |
| P3 | 文字数制限が USECASE 側のみ | Q-07 として積み残し |
